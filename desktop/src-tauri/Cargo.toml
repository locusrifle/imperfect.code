[package]
name = "imperfect-desktop"
version = "0.3.0"
description = "Window around the imperfect.computer machine"
edition = "2021"
publish = false

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
panic = "abort"
lto = true
