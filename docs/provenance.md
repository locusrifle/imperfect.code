# Provenance

This repository started as a clean product import, not an export of personal git history.

| Field | Value |
|---|---|
| Source tree | `/home/noah/projects/guey-pi` |
| Source commit | `82cbd4b3f92249499fd9a9f2fe2051f5e45c0f86` |
| Source subject | Prune what the laptop was for |
| Snapshot UTC | 2026-09-14T02:17:33Z |
| Worktree at snapshot | clean |
| File hashes | `/home/noah/output/imperfect-computer-setup/import-hashes.txt` |

Imported: `server.mjs`, `native/*.mjs`, `native/public/**` (every page, stylesheet, script, font, scene still), `native/test/**`, `test/**`, `extensions/guey-live/**`, `themes/garden.json`, `package.json`, `package-lock.json`, `native/antiburn-bridge` source.

Excluded: `node_modules`, credentials, `.env`, session archives, customer data, original personal maps (`AGENTS.md` / `PROJECT.md` / `skills/guey-update`), `records/`, `plans/`, `deploy/box/` operator keys and pilot tooling, `desktop/` laptop launcher, `dist/`.

The vaita packaged copy at audit time still contained voice/publishing/omarchy modules this import does not. Those were already removed in the source commit above. They are not restored here.

Laptop Node at import time was v26.7.0 on Arch. That binary is not the customer runtime.
